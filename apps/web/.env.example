# Copy to .env.local and fill in. apps/web reads these at build/runtime.
# Resend API key for the Crucible waitlist (POST /api/waitlist) and the feedback form (POST /api/feedback). Get one at https://resend.com.
RESEND_API_KEY=
