{
  "slug": "litellm",
  "name": "LiteLLM",
  "website": "https://www.litellm.ai",
  "logo": "/integrations/logos/litellm.png",
  "category": "replay-method",
  "categoryLabel": "Replay route · your own proxy",
  "title": "LiteLLM integration: replay through your own proxy",
  "description": "Point rightmodeler at a LiteLLM proxy as its replay route: candidate calls and the judge run through the gateway you already operate, on your own keys.",
  "h1": "rightmodeler + LiteLLM",
  "lede": "If a LiteLLM proxy already fronts your model traffic, it can be the route the audit replays through. rightmodeler needs an OpenAI-compatible base URL and the name of the variable holding its key, and your proxy is exactly that.",
  "tldr": "rightmodeler talks to whatever OpenAI-compatible endpoint you name with --base-url, reading the key from the variable you name with --api-key-env, so a LiteLLM proxy is a first-class replay route: GET /models lists the candidates and POST /chat/completions runs the replays and the judge. LiteLLM's /v1/models lists model ids only, so pass --catalog-reference with the upstream's public model list for price, context window, and capabilities, and name each model group by its upstream id so the entries join; when no model is priced after the join, rightmodeler reads per-token costs from the proxy's GET /model/info.",
  "steps": [
    {
      "label": "01 · Point",
      "title": "Name the proxy and the key variable",
      "body": "Start or reuse your gateway, then pass rightmodeler its base URL and the name of the environment variable holding a virtual key: --base-url http://localhost:4000/v1 --api-key-env LITELLM_API_KEY. The value is read at call time, never written to the store, and stripped from provider error excerpts before anything is logged."
    },
    {
      "label": "02 · Catalog",
      "title": "The shortlist comes from your model list",
      "body": "GET /models is read as the catalog. A candidate qualifies only when it is strictly cheaper than the incumbent on a blended per-token price, supports what the step needs, and has a context window at least as large as the step's observed tokens. LiteLLM's /v1/models lists ids only, so pass --catalog-reference with the upstream's public model list, such as https://openrouter.ai/api/v1/models, and name each model group by its upstream id, such as openai/gpt-4o-mini: rightmodeler joins the two by id and fills in each listed model's price, context window, output ceiling, and tool and structured-output support. When no model is priced after the join, it requests GET /model/info on the same host for LiteLLM's per-token costs. Entries still without a price cannot be compared and are left out, named in a catalog_reference_unmatched warning."
    },
    {
      "label": "03 · Replay + judge",
      "title": "Your route, the same evidence contract",
      "body": "Candidates replay the step's recorded inputs through POST /chat/completions on your proxy, and the built-in judge makes two temperature-zero calls with the reference and candidate positions swapped, chosen from a family that is neither the candidate's nor the incumbent's. Rate limits are retried with backoff and the concurrency cap adapts down rather than hammering your gateway."
    },
    {
      "label": "04 · Confirm",
      "title": "Coupled steps run in a container",
      "body": "A step whose output can feed later model-authored steps is confirmed by running your pipeline inside a container image you name in a --modeb-config file, with your app mounted read-only. Model calls leave through rightmodeler's own metered proxy, which swaps the model per step, meters every attempt, and refuses a request carrying no correlation header. Your LiteLLM gateway is the replay route, not the swap mechanism."
    }
  ],
  "setup": {
    "intro": "There is nothing to install into your app. If you already run a LiteLLM proxy, the audit can use it as the replay route; if you do not, any other OpenAI-compatible endpoint works the same way.",
    "commands": [
      {
        "comment": "# start (or reuse) the gateway you already operate",
        "command": "litellm --config config.yaml"
      },
      {
        "comment": "# project the replay spend through your proxy first",
        "command": "npx rightmodeler estimate --traces ./traces/spans.json --base-url http://localhost:4000/v1 --api-key-env LITELLM_API_KEY --catalog-reference https://openrouter.ai/api/v1/models"
      },
      {
        "comment": "# then run the pipeline under a hard spend cap",
        "command": "npx rightmodeler init --traces ./traces/spans.json --base-url http://localhost:4000/v1 --api-key-env LITELLM_API_KEY --catalog-reference https://openrouter.ai/api/v1/models --max-cost-usd 25"
      }
    ]
  },
  "reads": [],
  "detection": "",
  "useCases": [
    {
      "title": "Reuse the gateway you already run",
      "body": "If LiteLLM already fronts your model traffic, the audit does not need a second account or a second key. The same virtual key, budgets, and provider routing you already configured apply to the replay and judge calls too."
    },
    {
      "title": "Keep replay traffic inside your own perimeter",
      "body": "Replays are ordinary chat completions against the base URL you name, so a self-hosted proxy keeps recorded inputs on infrastructure you control rather than sending them to a route you do not operate."
    },
    {
      "title": "Bring your own provider mix",
      "body": "Because the shortlist is built from the models your proxy lists, the candidates are the models you have actually enabled, instead of every model in a public catalog you do not buy from."
    }
  ],
  "limits": [
    "rightmodeler has no LiteLLM log adapter. Proxy logs are not a supported trace format; if you want traces from that traffic, export OpenTelemetry GenAI spans and use the OTel adapter, or log OpenAI SDK calls in the JSONL shape that adapter reads.",
    "The shortlist needs a price and a context window for every candidate. LiteLLM's /v1/models carries neither, and GET /model/info supplies per-token prices and output ceilings but not context windows, so a run without --catalog-reference finds no qualifying candidates; the audit will not compare models on a price or a context window it had to invent. Name model groups by their upstream ids so the reference can match them.",
    "Cost comes from what the response body reports. A proxy that returns no cost field leaves rightmodeler computing the amount from catalog prices and token counts, and the ledger labels that figure an estimate.",
    "One replay endpoint is configured per run. A LiteLLM proxy, OpenRouter, and the Vercel AI Gateway are all OpenAI-compatible routes rightmodeler accepts; it does not mix keys within a run.",
    "Replays are not bit-exact, even at a fixed temperature. Repeated cases are read as a distribution and the release decision binds on a worst-case lower bound rather than a single sample.",
    "The proxy is a route, not a hop in production. rightmodeler runs offline on traces you exported and never sits in your request path."
  ],
  "faq": [
    {
      "q": "Do I need to run LiteLLM to use rightmodeler?",
      "a": "No. rightmodeler needs an OpenAI-compatible base URL and a key variable name; a LiteLLM proxy is one option, OpenRouter and the Vercel AI Gateway are others. Choose the route you already fund and operate."
    },
    {
      "q": "Can rightmodeler ingest my LiteLLM proxy logs?",
      "a": "Not directly. There is no LiteLLM trace adapter. The supported paths for that traffic are OpenTelemetry GenAI spans, read by the OTel adapter, or a per-call JSONL log in the OpenAI SDK shape, read by that adapter."
    },
    {
      "q": "Why does my run find no candidates through my proxy?",
      "a": "Almost always because the catalog has no price or context window to compare. LiteLLM's /v1/models lists ids only, and the shortlist needs a candidate that is strictly cheaper than the incumbent on a blended per-token price and has a context window at least as large as the step's observed tokens. Pass --catalog-reference with the upstream's public model list and name each model group by its upstream id; rightmodeler falls back to the proxy's GET /model/info for prices when nothing is priced after the join, and a catalog_reference_unmatched warning names any model the reference did not match."
    },
    {
      "q": "Does rightmodeler replace LiteLLM?",
      "a": "No, they do different jobs. LiteLLM routes live traffic; rightmodeler is an offline audit that borrows a route during replay and never sits in your request path. If a LiteLLM gateway already fronts your traffic, keep it."
    },
    {
      "q": "How does the end-to-end confirmation swap the model?",
      "a": "Not through your proxy config. Confirmation runs your pipeline in a container and routes its model calls through rightmodeler's own metered proxy, which applies the swap per step, charges every attempt, and fails closed on a request that arrives without correlation metadata rather than guessing which step it belonged to."
    },
    {
      "q": "Will the end-to-end pass always confirm a swap?",
      "a": "No, and that is why it runs. Confirmation exists to kill shortlisted swaps that cascade: a candidate that drifts mid-pipeline fails here even when its isolated outputs looked fine, and an inconclusive result is a legitimate terminal outcome rather than a pass."
    }
  ],
  "related": ["openrouter", "vercel-ai-gateway", "openai-sdk"],
  "cta": "github"
}
