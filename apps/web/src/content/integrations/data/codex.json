{
  "slug": "codex",
  "name": "Codex CLI",
  "category": "trace-source",
  "categoryLabel": "Trace source · dedicated adapter",
  "title": "Codex CLI integration: replay sessions on cheaper models",
  "description": "Point rightmodeler at ~/.codex/sessions and it replays each step of your Codex CLI rollouts through cheaper models, judging every output against what you accepted.",
  "h1": "rightmodeler + Codex CLI",
  "lede": "Codex CLI already writes every session to disk as a rollout log. rightmodeler reads those files where they sit and proves, step by step, which calls a cheaper model could have handled.",
  "tldr": "rightmodeler ingests Codex CLI rollout logs straight from ~/.codex/sessions with a dedicated adapter, no export step, because the JSONL files are already on your machine. Each successful step is replayed through cheaper candidates on your selected replay provider and judged against the output you already accepted, so everyday Codex sessions become the evidence for which model each kind of step really needs.",
  "steps": [
    {
      "label": "01 · Locate",
      "title": "Your sessions are already on disk",
      "body": "Codex CLI writes each session to ~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl as you work. There is nothing to export and nothing to instrument; choose one rollout, a day's folder, or the whole directory."
    },
    {
      "label": "02 · Ingest",
      "title": "Autodetected, then normalized",
      "body": "The ingester recognizes a rollout by its {timestamp, type, payload} lines. It takes the model from turn_context, folds message payloads into LLM steps, and pairs every function_call with its function_call_output by call_id, so the flat log becomes one per-step schema."
    },
    {
      "label": "03 · Replay + judge",
      "title": "Cheaper candidates, judged against what you accepted",
      "body": "Each successful single-shot step is re-run on cheaper candidates from your selected replay provider using the exact inputs from the rollout, and a cross-family judge scores every candidate against the answer Codex already gave, holding a 0.90 quality floor by default. Steps that called tools or looped are confirmed by executing real pipeline code end-to-end in a throwaway git worktree."
    },
    {
      "label": "04 · Approve",
      "title": "A report, and the call stays yours",
      "body": "The report itemizes each proposed swap with evidence and confidence, and the interactive TUI steps you through approve, reject, or hold, one step at a time. Thin evidence means abstain, and high-risk families like auth and secrets are flagged, not swapped."
    }
  ],
  "setup": {
    "intro": "Install once, then hand the skill a rollout file or the whole sessions directory. Nothing connects to your OpenAI account; the audit runs on files Codex already wrote.",
    "commands": [
      {
        "comment": "# list your most recent Codex rollouts",
        "command": "ls -t ~/.codex/sessions/*/*/*/rollout-*.jsonl | head -5"
      },
      {
        "comment": "# inside Claude Code, point the skill at your sessions",
        "command": "claude \"run the rightmodeler skill on ~/.codex/sessions\""
      }
    ]
  },
  "reads": [
    {
      "field": "step kind",
      "source": "payload.type"
    },
    {
      "field": "model",
      "source": "turn_context.model"
    },
    {
      "field": "input messages",
      "source": "payload.content[]"
    },
    {
      "field": "tool calls",
      "source": "function_call"
    },
    {
      "field": "tool results",
      "source": "function_call_output (call_id)"
    },
    {
      "field": "token usage",
      "source": "event_msg.token_count.usage"
    },
    {
      "field": "success signal",
      "source": "status / approval"
    },
    {
      "field": "historical cost",
      "source": "not present in Codex logs"
    }
  ],
  "detection": "timestamp · type · payload.type",
  "useCases": [
    {
      "title": "Audit a month of coding sessions",
      "body": "Rollouts accumulate under one dated tree, so the audit can take a day, a week, or the whole directory in one pass, and show which step families genuinely earned the model they ran on, across everything you did."
    },
    {
      "title": "Evidence before you change the default model",
      "body": "Instead of flipping your Codex model config and watching for regressions, replay the sessions you already ran and read the per-step verdicts first. The decision gets made on evidence from your own work, not a leaderboard."
    },
    {
      "title": "Compare candidate replay prices",
      "body": "Codex logs token counts, not cost, so missing historical step costs stay unknown. The audit prices candidate calls through your selected replay provider and shows which swaps preserve the output you accepted."
    }
  ],
  "limits": [
    "It reads rollout files from disk. It does not hook into Codex, watch live sessions, or change your Codex configuration; acting on the report stays your move.",
    "Steps where Codex called tools (shell commands, patches, file edits) are not judged from the log alone. They are confirmed by re-running real pipeline code end-to-end, and when a session cannot be re-run that way, the audit abstains on those steps.",
    "Rollouts do not record the set of tools Codex had available, so candidates are filtered on what each step actually did rather than the full declared tool surface.",
    "Codex does not log dollar cost. Missing historical step costs stay unknown rather than being derived from a provider catalog.",
    "It is a report, not a gateway. Nothing sits between Codex and its provider, and there is no rightmodeler server to see your sessions."
  ],
  "faq": [
    {
      "q": "Where does Codex CLI keep its session logs?",
      "a": "In ~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl, one JSONL file per session, written as you work. Hand the skill a single rollout, a day's folder, or the whole directory; the format is autodetected from the {timestamp, type, payload} line shape."
    },
    {
      "q": "Do I need to export anything before running the audit?",
      "a": "No. Codex already writes its rollouts to local disk, so there is no dashboard to visit and no export API to call. The only new credential is your replay provider key, used to run the replays."
    },
    {
      "q": "Does rightmodeler change which model Codex uses?",
      "a": "No. It produces a per-step report and an approve/reject/hold TUI; updating your Codex configuration afterwards is your call. Nothing hooks into Codex or intercepts its traffic."
    },
    {
      "q": "Does my session data leave my machine?",
      "a": "Only during replay, when step inputs go to candidate models through your selected replay provider under your own key, the same class of call Codex made to produce the log in the first place. There is no rightmodeler server and no account."
    },
    {
      "q": "Will it always find a cheaper model?",
      "a": "No, and that is the point. When the judge cannot establish equivalence, or a step touches auth, secrets, deploys, or similar, the audit abstains and the current model stays. A tool that always finds a swap is not measuring anything."
    },
    {
      "q": "What about steps where Codex ran commands or edited files?",
      "a": "Those are tool steps, and they are never judged from the log alone. They are confirmed by re-running real pipeline code end-to-end in a throwaway git worktree with the candidate model injected; where that re-run is not possible, the audit abstains."
    }
  ],
  "related": ["claude-code", "openai-sdk", "openrouter"],
  "cta": "github"
}
