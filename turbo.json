{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "!.next/dev/**",
        "build/**",
        "dist/**"
      ]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "format": {
      "cache": false
    },
    "check": {
      "dependsOn": ["^build", "^check"]
    },
    "@rightmodeler/cli#check": {
      "dependsOn": ["^build", "^check"],
      // Temporary Phase E2 parity inputs; shrink after the Phase E3 cutover removes the Python pipeline.
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/harness/docs/parity.md",
        "$TURBO_ROOT$/harness/fixtures/canonicalization/**",
        "$TURBO_ROOT$/apps/pipeline/**"
      ]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
