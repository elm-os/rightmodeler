# Demo app fixture

This mixed TypeScript and Python fixture exercises scanner coverage. Its repository metadata
also covers owner resolution and host conventions: ordered CODEOWNERS rules, root and nested
agent instructions, an included convention file, an instruction pointer, a pull request
template, and Prettier configuration.
