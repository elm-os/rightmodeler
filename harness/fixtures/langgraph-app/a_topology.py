def register_classify(builder, classify):
    builder.add_node("classify", classify)


def register_lookup(builder, lookup):
    builder.add_node("lookup", lookup)


def register_answer(builder, answer):
    builder.add_node("answer", answer)
