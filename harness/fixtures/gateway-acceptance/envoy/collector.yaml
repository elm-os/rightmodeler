receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318
exporters:
  file:
    path: /out/spans.jsonl
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [file]
