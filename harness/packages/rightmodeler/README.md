# Rightmodeler CLI

Published on npm as [`rightmodeler`](https://www.npmjs.com/package/rightmodeler); inside this
repository the workspace package is named `@rightmodeler/cli` so it cannot collide with the
repository root package.

Start with [the packaged getting-started guide](docs/getting-started.md).
