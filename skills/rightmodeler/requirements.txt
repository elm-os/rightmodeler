# rightmodeler skill deps
# Keep this fallback file for plain Python installs. The canonical dev setup is uv.
httpx>=0.27
rich>=13.7
textual>=0.79
# optional adapters (imported lazily; only needed for those features)
# langsmith>=0.1        # richer LangSmith trace ingestion
# litellm>=1.40         # LiteLLM proxy for code-execution model injection
# agentevals>=0.0.5     # trajectory match for E2E replay
