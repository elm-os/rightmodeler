# API key for the OpenAI-compatible endpoint passed with --base-url.
# Your model provider's key, not a rightmodeler key: this is only the default variable name (--api-key-env <NAME> reads another), and the CLI does not load .env itself.
RIGHTMODELER_API_KEY=
